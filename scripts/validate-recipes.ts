// Phase 6: will validate every /recipes/*.md against lib/schema.ts and
// exit non-zero naming any invalid file. No recipes or schema exist yet.
console.log("validate-recipes: nothing to validate yet");

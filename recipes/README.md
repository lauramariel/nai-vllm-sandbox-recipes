Submitted recipe files live here as `<slug>.md` — YAML frontmatter +
Markdown notes, validated against `lib/schema.ts`. Files are added by PRs
opened through `/api/submit` (see the design doc), not edited by hand in
the ordinary case.

This directory is intentionally empty until the first recipe is merged.
